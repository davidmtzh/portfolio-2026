# Optional public hosted-form endpoint, e.g. a verified Formspree form URL.
# This is a browser-visible URL, never a private API key.
# Without it, the form opens an email draft and offers copy actions.
VITE_CONTACT_ENDPOINT=
