# Before the cinematic 3D workstation

This folder contains the exact src snapshot from before the 3D workstation iteration. It includes the prior desk composition and rainbow trail.

To restore: copy this snapshot's src contents over new-site/src and remove the newly added src/components/WorkstationScene.jsx. No dependencies or public artwork were changed during this iteration. The earlier pre-desk-experiment snapshot is also retained separately.
